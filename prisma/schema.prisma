// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

<<<<<<< HEAD
model Type_users {
  id  Int  @id @default(autoincrement())
  description String  @unique
  users  Users[]

  @@map("type_users")
}

model Users {
  id          Int      @id @default(autoincrement())
  username    String
  phomeNumber String
  type_users   Type_users @relation(fields: [id], references: [id])

   @@map("users")
=======
model TypeUsers {
  id Int @default(autoincrement()) @id
  description String  @unique
  user Users[]
}

model Users {
  id Int @id @default(autoincrement())
  username String
  phomeNumber String
  typeUsers TypeUsers @relation(fields: [id], references: [id])
>>>>>>> 077fd25 (DOCB - Create project's archicture)
}




